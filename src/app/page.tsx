/**
 * Root route "/". Implementation lives in (marketing)/LandingPage.tsx
 * so the marketing group can own the content while the URL is explicit here.
 */
export { default } from "./(marketing)/page";
